[build-system]
# Minimum requirements for the build system to execute.
requires = ["setuptools>=42", "wheel", "setuptools_scm[toml]>=3.4", 
            "cython", "numpy"]
build-backend = "setuptools.build_meta"

[tool.setuptools_scm]
write_to = "numina/version.py"
